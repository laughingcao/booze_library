<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />
    <title>BoozeLibrary</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/main.css" /> 
    <link rel="icon" href="/images/favicon-32x32" type="image/png" />
    <link rel="shortcut icon" href="/favicon.ico" type=“image/x-icon”/> 
  </head>
  <body>
    <div class="wrapper">
      <div class="container">
        <%= "<div class='notice'>#{flash[:notice]}</div>" if
          flash[:notice] %>
        <nav>
          <% if logged_in? %>
          Logged in as: <%= current_user.email %> | 
          <a href="/bottles">Your Booze Library</a> |
          <a href="/bottles/new">Add Bottles</a> |
          <a href="/logout">Logout</a>
          <% else %>
          <a href="/register">Register</a> |
          <a href="/login">Login</a>
          <% end %>
        </nav> 
      <%= yield %>
      <footer class="branding">
        <small>&copy; 2021 <strong>BoozeLibrary</strong></small>
      </footer>
    </div>
  </body>
</html>
